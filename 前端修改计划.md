# DesktopPet 前端修改计划

> **版本**: 2.0  · **更新**: 2026-07-23
>
> 本版相对 1.0 的关键修正：配置 schema、字段名、main.cpp 示例全部对齐
> 现有 `ConfigManager`(`core/configLoader/`)与 `default_common_config.example.json`。
> 并经与项目负责人确认，对现有设计做 **3 处有控制扩展**（见 §十）。

---

## 一、架构概述

采用 **独立进程架构**：

```
┌─────────────────────────────────────────────────────────────┐
│                Python 启动器 (PySide6)                      │
│  - Fluent Design 启动器界面                                 │
│  - 负责配置管理（以 default_common_config.example.json 为模板)│
│  - 启动 C++ 核心进程                                        │
└──────────────────────────┬──────────────────────────────────┘
                           │
            导出 launch_config.json + 启动参数
                           ↓
         ┌──────────────────────────────────────────┐
         │         C++ DesktopPet 核心              │
         │  - main.cpp 增加 --config / --pet 参数   │
         │  - ConfigManager 加载配置（含新增 petSettings)│
         │  - MainWindow 接收角色名，启动时自动开宠   │
         └──────────────────────────────────────────┘
```

**核心约束（遵循现有设计）**：
- ✅ `ConfigManager` 的配置文件结构（`appSettings / renderSettings / interactionSettings / aiSettings` 四大块）**保持不变**，launcher 导出的文件必须与 `default_common_config.example.json` 同构。
- ✅ 角色注册表 `Pet`（`entity/pet.*`）与 `AppData/pets.json` 机制**不变**；launcher 仅通过 `--pet <name>` 入参指定启动角色。
- ✅ `MainWindow` 控制面板架构**不变**；`--pet` 仅让它启动时自动开宠，不绕过控制面板。
- ✅ C++ 运行时**不新增落盘逻辑**：配置流向仍是「加载时读 → 内存用」，launcher 是启动时的配置真相源。

---

## 二、launcher 功能清单（按真实配置字段对齐）

仅暴露**用户向字段**；`colliders`、相机、`behaviorPolicy`、`toolAccessPolicy`、`voice` 的 `venvPath/genieDataDir/workerScript` 等内部字段 **不进 UI**，由 launcher 从模板原样继承写回。

### 2.1 宠物设置页面

| 配置项 | UI 控件 | 对应配置字段 | 说明 |
|--------|--------|------|------|
| 角色选择 | 下拉框 | （不落配置，作为 `--pet` 入参） | 从 `Pet` 注册表读取；默认 `Milltina` |
| 大小/尺寸 | 滑块 + 数字输入 | `petSettings.scalePercent` | **新增字段**，1–200 |
| 窗口置顶 | 开关 | `petSettings.alwaysOnTop` | **新增字段** |
| 点击穿透 | 开关 | `petSettings.clickThrough` | **新增字段** |

> 「角色选择」对应角色注册表 `Pet::instance().getPetNames()`（数据源 `AppData/pets.json`），launcher 只读取展示供用户挑选，选中项通过 `--pet <name>` 传给 C++，不写入配置文件。

### 2.2 AI 设置页面

| 配置项 | UI 控件 | 对应配置字段 | 说明 |
|--------|--------|------|------|
| AI 开关 | 开关 | `aiSettings.profiles.default.enabled` | |
| 自动屏聊 | 开关 | `aiSettings.profiles.default.screenChat.enabled` | |
| 聊天间隔 | 数字输入（min/max） | `screenChat.minIntervalMs` / `maxIntervalMs` | 区间，非单值；默认 480000–720000 |
| **API 配置** | 卡片 | | |
| 提供商 | 下拉框 | `profiles.default.provider` | openai-compatible |
| Base URL | 输入框 | `profiles.default.baseUrl` | |
| API Key | 密码输入框 | `profiles.default.apiKey` | |
| 文本模型 | 输入框 | `profiles.default.model` | |
| 视觉模型 | 输入框 | `profiles.default.visual_model` | **下划线**命名 |

### 2.3 语音设置页面

| 配置项 | UI 控件 | 对应配置字段 | 说明 |
|--------|--------|------|------|
| 语音合成开关 | 开关 | `profiles.default.voice.enabled` | |
| 音量 | 滑块 | `appSettings.volume` | 真实为 0.0–1.0 浮点；UI 显示 0–100% 互转 |
| 说话者选择 | 下拉框 | `voice.selectedSpeaker` | 枚举：feibi / mika / thirtyseven |
| 音效开关 | 开关 | （由 `voice.sources.*` 或渲染层控制，见说明） | 见 §十一待确认 |

> `voice` 的 `backend/venvPath/workerScript/genieDataDir/characterModelsDir/speakerMode/sources` 等内部字段不暴露，从模板继承。「音效开关」当前 schema 无独立字段，见 §十一。

### 2.4 气泡/对话泡页面

> 气泡参数在真实 schema 中**不是独立段**，全部挂在 `aiSettings.profiles.default.screenChat` 下。

| 配置项 | UI 控件 | 对应配置字段 | 说明 |
|--------|--------|------|------|
| 气泡不透明度 | 滑块 + 数字 | `screenChat.bubbleOpacityPercent` | 0–100 |
| 气泡字体大小 | 数字输入 | `screenChat.bubbleFontSize` | px |
| 气泡水平偏移 | 数字输入 | `screenChat.bubbleOffsetX` | |
| 气泡垂直偏移 | 数字输入 | `screenChat.bubbleOffsetY` | |

### 2.5 高级设置页面

| 配置项 | UI 控件 | 对应配置字段 | 说明 |
|--------|--------|------|------|
| 主题切换 | 按钮/开关 | QSettings `ui/theme`（见 §三 主题共享） | 二者共用，C++ 零改动 |
| **渲染设置** | 卡片 | | |
| 抗锯齿 | 开关 | `renderSettings.antialiasing` | |
| 阴影质量 | 下拉框 | `renderSettings.shadowQuality` | low/medium/high |
| 纹理质量 | 下拉框 | `renderSettings.textureQuality` | low/medium/high |
| **交互设置** | 卡片 | | |
| 拖拽阈值 | 数字输入 | `interactionSettings.dragThreshold` | 像素 |
| 点击超时 | 数字输入 | `interactionSettings.clickTimeout` | 毫秒，默认 **200** |
| **窗口吸附** | 卡片 | `interactionSettings.windowSnapping` | |
| 吸附开关 | 开关 | `windowSnapping.enabled` | |
| 吸附阈值 | 数字输入 | `windowSnapping.snapThreshold` | 像素 |
| 垂直偏移 | 数字输入 | `windowSnapping.verticalOffset` | 像素 |

> `windowSnapping` 的 `snapZoneOffset/snapZoneSize/followIntervalMs/forceExitOnBigScreenAlarm/totalWindowSitAnimations` 不暴露，从模板继承。

### 2.6 关于页面

| 内容 | 说明 |
|------|------|
| 项目名称 | DesktopPet |
| 版本号 | 读取 `app.applicationVersion()`（当前 1.0.0） |
| 作者 | Inoriac |
| 许可证 | 显示 LICENSE.md 内容 |
| GitHub 链接 | 跳转仓库 |

---

## 三、配置文件格式

launcher 导出的 `launch_config.json` **必须与 `config/default_common_config.example.json` 同构**（四大块 + `petSettings` 新增块）。即：launcher 先完整加载模板，覆盖用户向字段，再整体写出。

```json
{
  "appSettings": {
    "defaultFPS": 60,
    "animationSpeed": 1.0,
    "volume": 0.75
  },
  "renderSettings": {
    "antialiasing": true,
    "shadowQuality": "medium",
    "textureQuality": "high",
    "cameraSettings": {
      "defaultEye": [0.0, 3.0, 12.0],
      "defaultCenter": [0.0, 4.0, 0.0]
    }
  },
  "interactionSettings": {
    "dragThreshold": 5,
    "clickTimeout": 200,
    "windowSnapping": {
      "enabled": true,
      "snapThreshold": 30,
      "verticalOffset": 0,
      "snapZoneOffset": [0, -5],
      "snapZoneSize": [100, 10],
      "followIntervalMs": 16,
      "forceExitOnBigScreenAlarm": true,
      "totalWindowSitAnimations": 0
    },
    "colliders": [ /* 从模板原样继承，不修改 */ ]
  },
  "petSettings": {
    "scalePercent": 100,
    "alwaysOnTop": true,
    "clickThrough": false
  },
  "aiSettings": {
    "activeProfile": "default",
    "profiles": {
      "default": {
        "enabled": true,
        "provider": "openai-compatible",
        "baseUrl": "https://api.example.com/v1",
        "apiKey": "sk-xxx",
        "model": "gpt-4",
        "visual_model": "gpt-4v",
        "timeoutMs": 30000,
        "maxTokens": 512,
        "temperature": 0.7,
        "retryCount": 1,
        "thinkIntervalMs": 30000,
        "screenChat": {
          "enabled": false,
          "minIntervalMs": 480000,
          "maxIntervalMs": 720000,
          "bubbleOpacityPercent": 80,
          "bubbleFontSize": 14,
          "bubbleOffsetX": 0,
          "bubbleOffsetY": -20,
          "bubbleDurationMs": 8000,
          "petGender": "female"
        },
        "voice": {
          "enabled": true,
          "selectedSpeaker": "feibi",
          "backend": "genie-tts"
          /* venvPath/workerScript/genieDataDir/... 从模板原样继承 */
        },
        "toolAccessPolicy": { /* 从模板继承 */ },
        "behaviorPolicy":   { /* 从模板继承 */ },
        "extraParams": { "stream": false }
      }
    }
  }
}
```

> 字段名一律对齐真实 schema：`visual_model`（下划线）、`min/maxIntervalMs`（区间）、`bubbleOpacityPercent` 等。模板里不存在 `pet` 旧块——1.0 版计划中的 `pet.name/scale/alwaysOnTop/clickThrough` 扁平段是错误的，已被 `petSettings` 取代。

### 3.1 主题共享（二者共用，C++ 零改动）

主题**不写入 `launch_config.json`**，而是复用 C++ 既有持久化：`ThemeManager` 在构造时经
`QSettings().value("ui/theme", "light")` 读取主题，`setTheme()` 时经 `QSettings().setValue("ui/theme", …)`
写回（见 `ui/theme_manager.cpp` 的 `loadPersistedTheme/persistTheme`）。

launcher 通过**同名 QSettings** 接入这把键即可实现双向共用：
- launcher 启动时读 `ui/theme` 同步自己的主题开关；
- 用户在 launcher 改主题 → 写同一 `QSettings` 键 → C++ 下次启动 `ThemeManager` 自动读到；
- 反之用户在 C++ 控制面板切主题 → 写同一键 → launcher 下次启动读到。

**强约束(关键,易踩坑)**:两端的 QSettings 路径必须一致,即组织名/应用名逐字相同。当前 C++ 端(`main.cpp:23-24`):

```
app.setOrganizationName("Desktop Pet Team");
app.setApplicationName("Desktop Pet");
```

launcher 启动时**必须**设置完全相同的两个值,否则 QSettings 落到不同路径,QSettings → 读写互通失败。主题**不走 `--config` 命令行**,因此 `main.cpp` 无需为主题做任何改动。

---

## 四、C++ 修改要求

### 4.1 `main.cpp`：新增命令行参数解析

> ⚠️ 必须保留现有逻辑：**`QSurfaceFormat` 要在 `QApplication` 之前设置**（OpenGL 3.3 Core 上下文要求），`ThemeManager`、`StatisticManager`、`QDir::setCurrent(applicationDirPath()+"/..")` 顺序不变。

```cpp
#include <QApplication>
#include <QCommandLineParser>
#include <QDir>
#include <QSurfaceFormat>

#include "ui/mainwindow.h"
#include "ui/theme_manager.h"
#include "statistic_manager.h"

int main(int argc, char *argv[])
{
    // OpenGL 3.3 Core，上下文须在 QApplication 创建前设置
    QSurfaceFormat fmt;
    fmt.setVersion(3, 3);
    fmt.setProfile(QSurfaceFormat::CoreProfile);
    fmt.setDepthBufferSize(24);
    fmt.setStencilBufferSize(8);
    fmt.setSwapBehavior(QSurfaceFormat::DoubleBuffer);
    QSurfaceFormat::setDefaultFormat(fmt);

    QApplication app(argc, argv);
    app.setApplicationName("Desktop Pet");
    app.setOrganizationName("Desktop Pet Team");
    app.setApplicationVersion("1.0.0");
    ThemeManager::instance().applyTo(&app);

    QDir::setCurrent(QCoreApplication::applicationDirPath() + "/..");

    // —— 新增：命令行参数 ——
    QCommandLineParser parser;
    parser.setApplicationDescription("Desktop Pet Application");
    parser.addHelpOption();
    parser.addVersionOption();

    QCommandLineOption configOption("config",
        "Path to launch configuration file (absolute path recommended)", "config-file");
    QCommandLineOption petOption("pet",
        "Pet name to auto-start (must exist in pets.json registry)", "pet-name");
    parser.addOption(configOption);
    parser.addOption(petOption);
    parser.process(app);

    const QString configPath = parser.value(configOption); // 建议传绝对路径
    const QString petName    = parser.value(petOption);

    if (!configPath.isEmpty()) {
        // ConfigManager 构造时已自动 load 一次默认路径；
        // 此处二次 load，loadConfig 为整体替换，安全覆盖。
        ConfigManager::instance().loadConfig(configPath);
    }

    // 初始化统计系统（默认 log/statistics.json）
    StatisticManager::getInstance().initialize();

    MainWindow w(petName); // 见 4.2：可选角色名
    w.show();
    if (!petName.isEmpty()) {
        w.autoStartPet(); // 启动后自动开宠（角色无效则忽略，详见 4.2）
    }

    return app.exec();
}
```

要点：
- 用 **`QApplication`**（GUI），非 `QCoreApplication`——这是 1.0 版示例的关键错误。
- 不要使用 Qt5 的 `AA_EnableHighDpiScaling`（Qt6 已默认开启并废弃该枚举）。
- 相对路径注意：`QDir::setCurrent` 已把工作目录设到可执行文件上级（`build/bin` → `build`），故 `--config` **建议传绝对路径**以避免歧义。

### 4.2 `MainWindow`：可选角色名 + 启动时自动开宠

- 构造函数增加可选参数 `explicit MainWindow(const QString& autoStartPet = QString(), QWidget* parent = nullptr);`
- 新增 `void autoStartPet();`：若 `autoStartPet` 非空且 `Pet::instance().hasPet(autoStartPet)`，则复用现有 `OnStartPet()` 流程启动（选中该角色 → 启动）；否则静默忽略，回到现有「等待用户点启动」行为。
- **不绕过** `MainWindow`，托盘/控制面板逻辑全部保留（注：`trayicon` 类目前未被实例化，本计划不改变这一现状）。

### 4.3 `ConfigManager`：新增 `petSettings` 块

仅 3 处增量（详见 §十）：
- 新增成员：`int petScalePercent = 100;` `bool petAlwaysOnTop = true;` `bool petClickThrough = false;`
- 新增 getter：`getPetScalePercent() / isPetAlwaysOnTop() / isPetClickThrough()`。
- `loadConfig()` 解析新增 `petSettings`（缺失则用上述默认值，与现有 UI 硬编码初值一致）。
- `MainWindow` 初始化时用这三个 getter 覆盖 `sizeSlider` / `alwaysOnTopCheckBox` / `clickThroughCheckBox` 的硬编码初值（当前是 `setChecked(true)` / `setChecked(false)`）。

> 运行时改这三项仍只在内存生效（沿用现有 `OnSettingsChanged` 行为，不新增 `saveConfig`）；launcher 是下一次启动的真相源。

---

## 五、启动流程

```
1. 用户运行启动器 (Launcher)
2. Python 启动器打开 Fluent Design 界面
   - 【强制约束】launcher 启动时须与 C++ 端逐字一致地设置组织/应用名：
     setOrganizationName("Desktop Pet Team") + setApplicationName("Desktop Pet")
     （见 §三 主题共享）。否则 QSettings 路径不一致，主题共享失效。
3. 启动器初始化：读取宠物注册表 Pet 的 AppData/pets.json；
   加载 default_common_config.example.json 作模板；
   经同名 QSettings 读取当前主题（键 ui/theme）
4. 用户配置各项（仅用户向字段可见），选择角色
5. 用户点击 [启动宠物]
6. 启动器：
   a. 在内存模板上覆盖用户向字段
   b. 若用户改过主题，写入同名 QSettings 的 ui/theme 键（C++ 零改动即可继承）
   c. 写出 launch_config.json（绝对路径，如 <AppData>/launch_config.json）
   d. 启动 C++ 核心：DesktopPet --config <abs path> --pet <petName>
   e. 启动器退出或最小化到托盘
7. C++ 核心进程：
   a. 解析 --config / --pet（主题不走命令行，见 §三）
   b. ConfigManager.loadConfig(--config)（二次 load 覆盖默认）
   c. ThemeManager 构造时经 QSettings 读取主题（ui/theme），applyTo(app)
   d. MainWindow 构造，读取 petSettings 初值
   e. autoStartPet(--pet)：校验角色后复用 OnStartPet 启动
```

---

## 六、技术栈

| 组件 | 技术选择 | 说明 |
|------|----------|------|
| UI 框架 | PySide6-Fluent-Widgets | **PySide6 绑定的包名为 `PySide6-Fluent-Widgets`**（`pip install PySide6-Fluent-Widgets`），非 `PyQt-Fluent-Widgets` |
| Python 版本 | Python 3.10+ | |
| Qt 绑定 | PySide6 | 与 C++ 端 Qt6 对齐 |
| 样式主题 | 深色主题 (Dark Teal) | |
| 图标库 | 内置 Fluent Icons | |

### 打包风险
launcher 直接双击运行依赖目标机器具备 Python 3.10+ 与已安装依赖。1.0 版「打包方式暂不考虑」过于乐观，建议至少：
- 短期：提供 `pip install -r requirements.txt` + 启动脚本；
- 中期：用 PyInstaller 打成单 exe（`--onefile --windowed`），否则「双击启动器」对终端用户不成立。

---

## 七、目录结构（新增）

```
DesktopPet/
├── launcher/                    # 新增：Python 启动器
│   ├── main.py
│   ├── requirements.txt         # PySide6, PySide6-Fluent-Widgets
│   ├── config_loader.py         # 模板加载 + 用户字段覆盖 + 导出 launch_config.json
│   ├── pet_registry.py          # 读写 AppData/pets.json（角色列表）
│   ├── pages/
│   │   ├── pet_page.py
│   │   ├── ai_page.py
│   │   ├── voice_page.py
│   │   ├── bubble_page.py
│   │   ├── advanced_page.py
│   │   └── about_page.py
│   └── resources/icons/
│
├── config/
│   ├── default_common_config.example.json   # 模板（launcher 基底，含 YOUR_API_KEY 占位）
│   ├── default_common_config.json           # 入库的运行默认（见下方说明）
│   ├── animation_state_machine.json
│   ├── personalities.json
│   └── pet_info.json                        # 已废弃空文件（角色数据改在 AppData/pets.json）
│
└── <AppData>/launch_config.json             # launcher 生成的运行时配置（绝对路径）
```

> **`default_common_config.json` 入库(已确认)**:该文件用作非 launcher 直跑 C++ 时的默认配置(`ConfigManager` 默认加载路径),从 `.example` 派生、**去掉 `YOUR_API_KEY` 等占位符并填合理默认值后纳入仓库**,避免直接跑 C++ 时 `file.open` 失败而退回硬编码默认。它与 launcher 导出的 `<AppData>/launch_config.json` 是两条路径:`--config` 指向后者时覆盖前者。
> **`saveConfig` 不新增(已确认)**:保持 launcher 为配置真相源。C++ 运行时不回写配置文件——用户在 C++ 控制面板的临时改动不落盘,下次启动回到 `launch_config.json` 的值。该行为可接受(符合现有设计),未来若需闭环再议。

---

## 八、许可证兼容性

### PySide6-Fluent-Widgets (GPLv3)

| 组件 | 许可证 | 兼容情况 |
|------|--------|----------|
| DesktopPet 核心代码 | MateEngine Pro License | 开源非商业 ✅ |
| PySide6-Fluent-Widgets | GPLv3 | 开源非商业 ✅ |
| 整体项目 | 需保持开源 | 需遵守 GPLv3 |

**结论**：开源项目，兼容 GPLv3。

> ⚠️ 商业化需联系作者购买商业授权，或替换为 Qt 官方 FluentWinUI3（免费）。注：PySide6 本身为 LGPL/商业双授权，与 GPL 组件组合后整体受 GPL 约束。

---

## 九、后续步骤

1. **C++ 修改**（详见 §四、§十）：
   - `main.cpp` 加 `--config` / `--pet` 解析(保持 QSurfaceFormat 顺序;**主题零改动**,走 §3.1)。
   - `MainWindow` 可选角色名 + `autoStartPet()`。
   - `ConfigManager` 新增 `petSettings` 块 + getter;`MainWindow` 读取初值。
2. **仓库整理**:`config/default_common_config.json` 从 `.example` 派生并入库(去占位符)。
3. **Python 启动器开发**:建项目结构,按 §二 字段映射写各页面;实现模板加载 + 覆盖 + 导出;`setOrganizationName/ApplicationName` 逐字对齐 C++ 以打通主题 QSettings。
4. **配置对接联调**:launcher → `launch_config.json` → C++ `loadConfig` → `MainWindow` 初值 → `autoStartPet`。
5. **角色列表联调**:launcher 读写 `AppData/pets.json` 与 C++ `Pet` 注册表一致。
6. **主题联调**:launcher 改主题 → 写 QSettings `ui/theme` → C++ 启动读到;反向同理。

---

## 十、对现有设计的 3 处有控制扩展（已确认）

> 与项目负责人确认：以下扩展为使 launcher 可用而做，尽量最小侵入。

| # | 现有设计现状 | 扩展内容 | 影响面 |
|---|------|------|------|
| 1 | `scalePercent/alwaysOnTop/clickThrough` 仅是 `MainWindow` 运行时 UI 状态，**无持久化** | 新增 `petSettings` 配置块 + `ConfigManager` 字段 + `MainWindow` 读取初值 | `config_manager.{h,cpp}`、`mainwindow.cpp` 初始化处；运行时仍不落盘 |
| 2 | 角色由 `MainWindow` 内部选择启动，`main` 入口**无角色参数** | `main.cpp` 新增 `--pet <name>`；`MainWindow` 可选角色名 + 启动时自动开宠（复用 `OnStartPet`） | `main.cpp`、`mainwindow.{h,cpp}`；不绕过控制面板 |
| 3 | schema 仅含 4 大块 | launcher 仅暴露用户向字段，内部字段从模板继承 | 仅 launcher 行为约定，不改 C++ schema 约束 |

> 角色注册机制（`Pet` / `AppData/pets.json`）、`loadConfig` 整体替换语义、`MainWindow` 控制面板、4 大块 schema 结构 **均保持不变**。

---

## 十一、已确认结论与遗留

> 以下细节已与项目负责人确认,记录为结论(取代 1.0 版待确认列表):

- **音效开关**:**暂不实现**。项目当前未实现音效,本轮 UI 不纳入「音效开关」项,2.3 表中相关行标注为待实现、不映射字段。
- **主题切换**:二者共用,采用 §3.1 方案——launcher 与 C++ 经同名 QSettings(`ui/theme` 键)共享组织名/应用名一致即可,C++ **零改动**,不走 `launch_config.json`。
- **`saveConfig`**:**不新增**。保持 launcher 为配置真相源;C++ 运行时不回写文件,用户在 C++ 控制面板的临时改动不落盘,行为可接受。
- **`default_common_config.json`**:**入库**。从 `.example` 派生、去占位符后纳入仓库,作非 launcher 直跑 C++ 的默认配置。

---

## 十二、TODO:聊天窗 Python 化(另开分支)

> 与项目负责人确认:本轮**不动**聊天窗,留作后续在新分支单独开发。以下为届时需要评估的 C++ 能力暴露面。

**现状**(均为同进程直接调用):
- 右键菜单 `ui/petwindow_interaction.cpp:87`「打开聊天窗口」→ `PetWindow::openChatHistoryWindow()`(`ui/petwindow.cpp:238`)
- `ChatHistoryWindow`(`ui/chat_history_window.*`)为 C++ QWidget,展示 `chatHistoryEntries`
- 用户输入经 `messageSubmitted` 信号 → `aiBrain->triggerThink(text, "user_request")`(`petwindow.cpp:245`)
- 回复由 `appendChatHistoryMessage()` 持久化为 JSONL(`chatHistoryFilePath()`)并推送显示
- `AIBrain`(`core/ai/ai_brain.h`)有 `assistantResponseReady/thinkingStarted/thinkingFinished/toolExecuted` 等异步信号
- 聊天窗已接 `ThemeManager::themeChanged`(`chat_history_window.cpp:114`),风格本就统一

**风险**:聊天窗与 `AIBrain`/`PetWindow` 紧耦合(triggerThink、异步回复、气泡、语音 `speakPetReply` 均同进程)。Python 化 = 同进程对象拆成跨进程 RPC,工程量与故障面显著上升;唯一收益是 Fluent 风格,而现状已由 ThemeManager 统一。结论:**性价比低,谨慎评估**。

**若仍要做,需暴露/新增的 C++ 能力**(新分支评估):
1. 本地 IPC 通道(`QLocalServer` 最轻 或 嵌入式 HTTP);C++ 作 server,Python 聊天窗作 client。
2. RPC `submitUserMessage(text)` → 内部调 `triggerThink`。
3. 回复流:将 `assistantResponseReady/thinkingStarted/thinkingFinished/toolExecuted` 序列化推送至 Python。
4. 历史:提供 `getHistory()/appendHistory()` RPC(优于 Python 直写 JSONL,避免格式漂移)。
5. 气泡/语音联动:暴露 `showBubbleMessage`/`speakPetReply`。

---

**文档版本**: 2.1
**更新日期**: 2026-07-23