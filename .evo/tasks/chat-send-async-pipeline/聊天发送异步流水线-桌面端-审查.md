# 聊天发送异步流水线 Task 1 第三轮最终复审

## 审查结论

Task 1 通过。上一轮唯一剩余的 P1 restart 竞态已关闭；本轮未发现新的 P1/P2。

## 第三轮 P1 闭环

### stop 后立即 restart + submit

已关闭。

- `ChatPreparationExecutor::start()` 在旧 worker 仍处于 stopping 时记录最新的 pending restart environment，恢复 accepting，并返回成功；不会让 `AIBrain` 进入 running 但 executor 永久不可用的状态。
- 该窗口内的 `submit()` 只保留一个有界 pending request。旧 worker 的结果由 cancellation epoch 抑制；旧线程结束后，pending request 只向新 worker 提交一次。
- `stop_whenRestartIsRequestedDuringShutdown_shouldDispatchPendingRequestOnce` 覆盖 100ms 慢准备后不等待 shutdown，直接执行 `stop -> start -> submit`，断言只收到 pending request 的一个成功结果。该用例单独重复 20 次全部通过。

### restart 后再次 stop

已关闭。

- 每次 `stop()` 都先关闭 accepting、递增 cancellation epoch，并清空 pending restart environment 与 pending request；即使旧 worker 已经处于 stopping，也不会遗漏这一步。
- `finished` 回调只有在 accepting 仍为 true 且 pending restart environment 仍存在时才重启。第二次 stop 清空状态后，回调只回收旧 worker，不会复活线程或派发 pending request。
- `stop_whenPendingRestartIsStoppedAgain_shouldDiscardPendingRequest` 覆盖该时序，断言线程最终停止且结果数为 0。该用例单独重复 20 次全部通过。

## 生命周期与竞态复核

- `m_stopping` 一直保持到旧线程的 `finished` 回调执行 `finalizeStoppedWorker()`，所以外部 `start()` 无法在旧回调之前创建新 worker；旧 `finished` 回调不存在误回收新 worker 的窗口。
- shutdown 在 worker 线程关闭 memory/identity SQLite 资源，再把 Worker 迁回 executor 的 owner thread 并退出 QThread；`finished` 回调随后在 owner thread 删除 Worker。
- 析构调用 `stop()` 清除全部 pending 状态，再等待 queued shutdown 完成。等待返回后由 owner thread 回收 Worker；析构期间排入 executor 的迟到 result/finished 事件会随 QObject 销毁被移除。
- worker task 在执行前、准备后以及 owner-thread emit 前均检查共享 cancellation epoch，并以 `QPointer<ChatPreparationExecutor>` 守护回调。stop、二次 stop 和析构均不会让旧结果越过代际门禁，也未发现 pending request 的 UAF 路径。
- pending request 按值持有，restart 回调先将 pending 状态移入局部变量并清空成员，再启动新 worker 和提交，未发现重复提交或悬空引用。

## 既有问题闭环

- NeedLLM 显式 forget 在召回前以共享 `MemoryPolicy::matchesForgetQuery()` 同时过滤持久与 working memory，relation expansion 无法重新引入被过滤项；正常结果返回后仍执行最终删除。
- 正常 stop 立即返回，资源最终在线程内关闭，旧结果被抑制。
- runtime snapshot、legacy 路径、local router side effects、动态 prompt/baseline、relation graph、reinforcement ID 和 request generation 门禁保持正确。
- Task 1 相关测试不链接或执行 ONNX provider，未启动完整桌宠或加载 3D 模型。

## 验证记录

- `chat_preparation_executor_tests`：15 passed，0 failed。
- `streaming_dialogue_tests`：21 passed，0 failed。
- `agent_runtime_services_tests`：44 passed，0 failed。
- 两条新增 restart/second-stop 竞态用例各额外重复运行 20 次，全部通过。
- 测试二进制时间戳晚于本轮对应源码；当前 shell 无 `cmake` 命令，未在本次复审中再次重编译。
- `git diff --check`：通过。
- 未运行 ONNX、完整桌宠或 3D 资源相关路径。

---

# 聊天发送异步流水线 Task 2 最终复审

## 审查结论

Task 2 当前实现通过适度复审。首轮审查发现的发送关键路径同步写盘、GUI memory cache 不一致、关键副作用入队降级和常见线程生命周期问题均已修复；未发现仍会阻塞当前交付的 P1/P2。

## 关键修复闭环

- 用户记忆、显式忘记、Daydream impression 和工具结果使用 GUI 纯内存 staging，随后将不可变 `MemoryMutationBatch` 投递给副作用 worker；GUI 不再在模型派发前写 SQLite 或 legacy JSON，入队失败会回滚内存变更。
- `ChatSideEffectQueue` 使用默认容量 256 的 FIFO 队列，满载时优先丢弃非关键 request/response log，并为 session barrier 预留一个位置；关键任务拒绝会产生 warning，记忆 staging 会回滚。
- `stop(true)` 保持非阻塞；立即 restart 会等待旧 worker 在线程内关闭数据库后再创建新 worker。析构走 discard + 最长 1 秒的 cooperative wait，避免常见 owner/SQLite 生命周期越界。
- 每个模型 round 的 completion 由共享 once flag 去重；取消时在 barrier 前生成一次 synthetic response log，迟到或 provider 重复 callback 不会重复记录或修改新回复。
- 工具结果持久化不再阻塞下一轮模型派发。同步工具执行本身仍沿用现有状态机，README 已明确该边界和后续 executor/Agent Actor 触发条件。

## 验证记录

- `chat_side_effect_queue_tests`：15 passed，0 failed。
- `streaming_dialogue_tests`：29 passed，0 failed。
- `chat_preparation_executor_tests`：15 passed，0 failed。
- `memory_strategy_tests`：staging/强化相关 5 个指定用例通过（含 init/cleanup 共 7 passed）。
- `git diff --check`：通过。
- 按用户要求未运行仓库全量测试；未运行 ONNX、完整桌宠、OpenGL 或 3D 资源路径。

## 已知边界

- 工具实现本身仍可能同步占用 GUI 线程；本次只移除了工具结果的持久化等待。若代表性工具稳定超过一帧，应优先迁入独立 executor。
- 极端情况下若队列被关键任务持续填满，新关键任务会被明确拒绝并 warning；常规容量为 256，当前未为这种压力场景引入更重的持久化 outbox。
